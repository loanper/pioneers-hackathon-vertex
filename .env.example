# Pizza Pipeline - Configuration Environment
# Copy this file to .env and fill in your values

# Pizza Pipeline - Configuration Environment
# Copy this file to .env and fill in your values

# GCP Configuration
PROJECT_ID="build-unicorn25par-4813"
BILLING_ACCOUNT="YOUR-BILLING-ACCOUNT-ID"
REGION="europe-west1"

# Buckets (auto-generated from PROJECT_ID)
BUCKET_RAW="pz-audio-raw-${PROJECT_ID}"
BUCKET_PROC="pz-audio-processed-${PROJECT_ID}"
BUCKET_ANALYTICS="pz-analytics-${PROJECT_ID}"
BUCKET_REPORTS="pz-reports-${PROJECT_ID}"

# Service Account
SA="pipeline-sa"

# User Configuration
USER_TZ="Europe/Paris"

# Gemini 2.x Configuration (Google Gen AI SDK / Vertex AI)
# Models: gemini-2.0-flash-exp (fast/cheap demo), gemini-2.5-pro (advanced reasoning)
# Location: global (required for Gemini 2.5 and some 2.0 variants)
GEMINI_MODEL="gemini-2.0-flash-exp"
GOOGLE_CLOUD_LOCATION="global"

# Optional: For local development
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account-key.json"
